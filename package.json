{
    "private": true,
    "devDependencies": {
        "@zeit/ncc": "^0.21.0"
    },
    "dependencies": {
        "@actions/core": "^1.2.2",
        "@actions/exec": "^1.0.3",
        "@actions/github": "^2.0.1",
        "@octokit/webhooks": "^7.0.0",
        "now-client": "^6.0.1"
    },
    "scripts": {
        "build": "ncc build -m src/index.ts -o dist"
    }
}
